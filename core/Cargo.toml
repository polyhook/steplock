[package]
name = "steplock-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Stateful quality gate logic for steplock"
license = "MIT"
repository = "https://github.com/polyhook/steplock"
homepage = "https://github.com/polyhook/steplock"
readme = "README.md"
keywords = ["ai", "hooks", "quality-gate", "polyhook", "checklist"]
categories = ["development-tools", "command-line-utilities"]
exclude = ["Architecture.md", "deny.toml"]

[lints.rust]
missing_docs = "deny"

# Clippy lints are ratcheted to `deny` incrementally — every PR promotes at
# least one more (see .steplock/checklists/pre-push).
[lints.clippy]
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
manual_string_new = "deny"
uninlined_format_args = "deny"
cloned_instead_of_copied = "deny"
match_same_arms = "deny"
semicolon_if_nothing_returned = "deny"
if_not_else = "deny"
map_unwrap_or = "deny"
redundant_closure_for_method_calls = "deny"
needless_continue = "deny"
format_push_string = "deny"
missing_errors_doc = "deny"
must_use_candidate = "deny"
write_with_newline = "deny"
missing_const_for_fn = "deny"
needless_collect = "deny"
flat_map_option = "deny"
inefficient_to_string = "deny"
implicit_clone = "deny"
redundant_else = "deny"
use_self = "deny"
ref_option_ref = "deny"
cast_lossless = "deny"
checked_conversions = "deny"
from_iter_instead_of_collect = "deny"
doc_markdown = "deny"
str_to_string = "deny"
multiple_inherent_impl = "deny"

[dependencies]
polyhook = "0.1.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
cel-interpreter = "0.8"
uuid = { version = "1", features = ["v4"] }
thiserror = "1"
chrono = { version = "0.4", features = ["serde"] }

[[bin]]
name = "steplock"
path = "src/bin/main.rs"

[profile.release]
strip = "symbols"
lto = "thin"
codegen-units = 1

[dev-dependencies]
tempfile = "3"
